     apiVersion: v1

     kind: Pod

     metadata:
       name: myapp-pod
       labels:
         app: myApp
         costcenter: NorthAmer

     spec:
       containers:
         - name: nginx-container
           image: nginx
