---
    apiVersion: v1

    kind: ResourceQuota

    metadata:
      name: comp-quota
      namespace: dev

    spec:
      hard:
       pods: "10"
       requests.cpu: "2"
       requests.memory: 2Gi
       limits.cpu: "3"
       limits.memory: 3Gi
